// Import the Navigation component from './Routes'
import { Navigation } from './Routes';

// Define the main App component
export const App = () => {
    return (
        // Render the Navigation component, which includes your routes
        <Navigation />
    );
}

// Export the App component as the default export
export default App;
